<% provide(:title, 'All users') %>
<h1>All users</h1>


<%= will_paginate @users, :previous_label => 'prev'  %>


<ul class="users">
  <% @users.each do |user| %>
   <li>
  		<%= link_to user.name, user_path(user) %>
	</li>
  <% end %>
</ul>

<%= will_paginate %>
